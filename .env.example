# ── Shared URLs (single source of truth for all services) ──────────
# FRONTEND_URL: public URL where the frontend (Mini App) is served.
#   Production: https://fitness-tracker.synerr.dev   (Cloudflare Pages)
#   Dev:        leave empty when backend serves the SPA (same-origin)
FRONTEND_URL=https://fitness-tracker.synerr.dev

# BACKEND_URL: public URL where the API is served.
#   Production: https://fitness-backend.synerr.dev   (Coolify)
#   Dev:        same as FRONTEND_URL or http://localhost:3001
BACKEND_URL=https://fitness-backend.synerr.dev

# ── Backend ───────────────────────────────────────────────────────
BIND_ADDR=0.0.0.0:3001
FITNESS_EXERCISE_DB=/data/exercises.db
FITNESS_WORKOUT_DB=/data/workouts.db
FITNESS_HEALTH_DB=/data/health.db
RUST_LOG=info

# ── Bot ───────────────────────────────────────────────────────────
TELOXIDE_TOKEN=your-telegram-bot-token

# ── Frontend (baked at build time via Vite) ───────────────────────
# Set VITE_API_BASE to BACKEND_URL when frontend is on a different origin.
# Leave empty when the backend serves the SPA (dev / full-stack mode).
VITE_API_BASE=https://fitness-backend.synerr.dev

# ── Cloudflare Pages (configured via dashboard) ──────────────────
# Set VITE_API_BASE=https://fitness-backend.synerr.dev as an
# environment variable in the Cloudflare Pages project settings.
# Build command: npm run build    Output directory: dist

# ── CI / CD secrets (GitHub Actions) ─────────────────────────────
# COOLIFY_WEBHOOK_PROD=https://coolify.example.com/api/v1/deploy?...
# COOLIFY_WEBHOOK_DEV=https://coolify.example.com/api/v1/deploy?...
