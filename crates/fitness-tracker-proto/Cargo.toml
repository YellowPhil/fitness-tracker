[package]
name = "fitness-tracker-proto"
version = "0.1.0"
edition = "2024"

[dependencies]
tonic = { workspace = true }
prost = { workspace = true }

[build-dependencies]
tonic-build = { version = "0.12", features = ["prost"] }
